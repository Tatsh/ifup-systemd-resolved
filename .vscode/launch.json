{
  "configurations": [
    {
      "cwd": "${workspaceFolder}/build",
      "name": "Debug",
      "program": "${workspaceFolder}/build/ifup-systemd-resolved",
      "request": "launch",
      "type": "cppdbg"
    }
  ],
  "version": "0.2.0"
}
